services:
  etf-collector:
    image: ghcr.io/artiveloper/etfview-collector:latest
    build: .
    env_file:
      - .env
    environment:
      - LOG_DIR=/app/logs
    volumes:
      - ./logs:/app/logs
    user: "1001:1001"
    restart: unless-stopped
